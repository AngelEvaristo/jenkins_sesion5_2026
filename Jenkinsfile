@Library('shared-lib@main') _
pipeline {
    agent any
    stages {
        stage('prueba'){
            steps{
                script {
                    hello('dotnet')
                }
            }
        }
    }
}